# OpenSky Network credentials (create an account at opensky-network.org)
# With credentials the proxy can refresh data every 25s instead of 240s.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=

# Optional overrides (defaults are fine, comment out if not needed)
# RWR_OSAPI_TTL=25
# RWR_RATE_MAX=30
